// sections
import { UserCreateView } from 'src/sections/profile/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Create Your Profile',
};

export default function ProfileCreatePage() {
  return <UserCreateView />;
}
