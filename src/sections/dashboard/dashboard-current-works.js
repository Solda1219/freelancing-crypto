// @mui
import { Card, CardHeader, Divider, Stack } from '@mui/material';

export default function CurrentWorks() {
  return (
    <Card>
      <CardHeader title="Current Works" />
      <Divider sx={{ mt: 1.5 }} />
      <Stack p={3} spacing={3}>
        TODO
      </Stack>
    </Card>
  );
}
